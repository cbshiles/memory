package memory.tools;

import java.time.LocalDateTime;

public class Time {

    /**
     * Check if one datetime is between two others. (inclusive)
     * @param self  
     * @param start  
     * @param end 
     * @return Whether self is in between start and end
     */
    public static boolean isBetween(LocalDateTime self, LocalDateTime start, LocalDateTime end) {
	return true;
    }
}
